{
  "name": "ships-api",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "version": "1.0.0",
  "description": "REST API for managing ships, built with Node.js, Express, and MongoDB.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "smoke": "node scripts/smoke.js",
    "test:api": "newman run postman/Ships-API.postman_collection.json -e postman/Ships-API.postman_environment.json --color on --timeout-request 10000",
    "test:api:report": "newman run postman/Ships-API.postman_collection.json -e postman/Ships-API.postman_environment.json --reporters cli,html,junit --reporter-html-export postman/report.html --reporter-junit-export postman/report.xml --color on --timeout-request 10000"
  },
  "keywords": [
    "ships",
    "api",
    "express",
    "mongodb",
    "rest"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.2.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.5"
  },
  "devDependencies": {
    "newman": "^6.2.1",
    "nodemon": "^3.1.0"
  }
}
